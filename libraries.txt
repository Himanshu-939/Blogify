backend : express, mongoose, nodemon, jsonwebtoken, 
bcryptjs(used to hash the password)

frontend : react vite, react-router-dom

shadcn for vite, react icons, redux, 
npm redux persist -> do not log out on reload